export type CameraCreateFields = {
  title?: string;
  active?: string;
  external_link?: string;
  description?: string;
};

export type CameraEditFields = {
  title?: string;
  active?: string;
  external_link?: string;
  description?: string;
  id: number;
};
