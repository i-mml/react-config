import React from 'react'
import s from './style.module.scss';

const CameraListItem = () => {
    return (
        <div className={s.container}>CameraListItem</div>
    )
}

export default CameraListItem