import React from "react";
import s from "../support.module.scss";

const ExternalSupport = () => {
  return <div className={s.externalContainer}>ExternalSupport</div>;
};

export default ExternalSupport;
